// Firebase web-app config. Leave null to run with a local, on-device stand-in
// (no shared board). Paste the object from the Firebase console to go live.
// These values are safe to publish: Firestore security rules do the protecting.
window.FIREBASE_CONFIG = null;
